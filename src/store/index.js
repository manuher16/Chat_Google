import { createStore } from 'vuex'
import session from '../modules/session'
export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    session
  }
})
